import {NotifyConfig} from '@angular-boot/util/lib/nb-util/toolkit/notify';

export namespace NotiConfig {
    export const notifyConfig: NotifyConfig = {
        options: {},
        settings: {
            placement: {align: 'right', from: 'top'}
        }
    };

}
